build-pdfs:
  rules:
    - changes:
        - resume.md
        - system-design.md
        - preamble.tex
        - flake.nix
        - Makefile
        - filters/**/*.lua
        - templates/**/*
        - .gitlab-ci.yml
  image: nixos/nix:latest
  before_script:
    - echo "experimental-features = nix-command flakes" >> /etc/nix/nix.conf
    - echo "substituters = https://cache.nixos.org https://$CACHIX_CACHE.cachix.org" >> /etc/nix/nix.conf
    - echo "trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= $CACHIX_PUBLIC_KEY" >> /etc/nix/nix.conf
    - nix-env -iA nixpkgs.cachix
    - cachix authtoken $CACHIX_AUTH_TOKEN
  script:
    - nix build .#system-design-pdf --out-link result-system-design
    - nix build .#resume-pdf        --out-link result-resume
    - cachix push $CACHIX_CACHE result-system-design result-resume
  artifacts:
    paths:
      - result-system-design/system-design.pdf
      - result-resume/resume.pdf
    when: on_success
  after_script:
    - |
      if [ "$CI_JOB_STATUS" = "success" ] && [ "$CI_COMMIT_BRANCH" = "main" ]; then
        cp result-system-design/system-design.pdf system-design.pdf
        cp result-resume/resume.pdf resume.pdf
        git config user.name  "gitlab-ci-bot"
        git config user.email "gitlab-ci-bot@noreply.gitlab.com"
        git remote set-url origin "https://oauth2:${GITLAB_TOKEN}@${CI_SERVER_HOST}/${CI_PROJECT_PATH}.git"
        git add system-design.pdf resume.pdf
        git diff --staged --quiet || git commit -m "ci: rebuild PDFs [skip ci]"
        git push origin main
      fi
